# Prod dependencies
fastapi
uvicorn[standard]
httpx
loguru
python-dotenv
tiktoken

# AWS cloud-native backend (only required when STORAGE_BACKEND=aws)
boto3

# Testing dependencies
pytest
pytest-asyncio
hypothesis
moto[ssm,s3]
